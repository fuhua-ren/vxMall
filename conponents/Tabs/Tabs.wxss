.tabs_title {
  display: flex;

}

.title_item {
  display: flex;
  justify-content: center;
  align-content: center;
  flex: 1;
  padding: 15px 0;
}

.active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}